{% extends 'base.html.twig' %}

{% block title %}City{% endblock %}

{% block body %}
    <style>
        .btn-back{
            margin-bottom: 2vh;
        }
    </style>

    <h1>City</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ city.name }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td><img src="{{ asset(city.photo) }}" alt="{{ city.photo }}" style="width: 300px; height: 200px "></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_city_crud_index') }}" class="btn btn-secondary btn-back">back to list</a>

{#    <a href="{{ path('app_city_crud_edit', {'id': city.id}) }}">edit</a>#}

{#    {{ include('city_crud/_delete_form.html.twig') }}#}
{% endblock %}
